namespace KSM_ULS
{
    public partial class AppShell : Shell
    {
        public AppShell()
        {
            InitializeComponent();

            // Routing.RegisterRoute(nameof(ClienteDetallePage), typeof(Pages.ClienteDetallePage));
        }
    }
}
