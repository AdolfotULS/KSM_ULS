<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:pages="clr-namespace:KSM_ULS.Pages"
       x:Class="KSM_ULS.AppShell">


    <!-- 
    Prueba 1: Mostrar Garantías 

    <!-- Puedes añadir más secciones (Clientes, Órdenes) luego 
    <ShellContent Title="Dashboard"
                ContentTemplate="{DataTemplate pages:LoginPage}" />
    
    -->

    
    <TabBar>
        <ShellContent Title="Garantías"
                      ContentTemplate="{DataTemplate pages:GarantiasPage}" />
    </TabBar>
    
    

    <!-- 
    Prueba 2: Mostrar Dashboard
    
    -->
    
    <!--
    <TabBar>
        <ShellContent Title="Dashboard"
                      ContentTemplate="{DataTemplate pages:DashboardPage}" />
    </TabBar>
    -->


</Shell>
